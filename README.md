# Gradify — mp3quran + Full surahs + Search
- يولّد تلقائيًا روابط 001..114 لعدة قراء من mp3quran.net عبر `host` و `path`.
- عدّل القراء من `READERS` في `script.js`. مثال:
  { id:'basit', name:'عبد الباسط', host:'server7.mp3quran.net', path:'basit' }
- محرك بحث داخلي باسم السورة أو رقمها.
