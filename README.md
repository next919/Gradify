# قالب صفحة واحدة (عربي، حديث وخفيف)

هذا مشروع جاهز لصفحة واحدة متجاوبة وخفيفة يعمل على أي استضافة ملفات ثابتة (Static Hosting).

## المزايا
- بدون مكتبات ثقيلة؛ HTML + CSS + JS فقط.
- أداء سريع مع صور _webp_ وتحميل كسول.
- تحسين أساسي للسيو وإمكانية الوصول (ARIA، ألوان متباينة، meta).
- قائمة تتبع القسم الحالي + حركات خفيفة عند التمرير.
- نموذج تواصل متوافق مع **Netlify Forms** مباشرة.

## الملفات
- `index.html` – الصفحة الرئيسية مع الأقسام.
- `styles.css` – تنسيق متجاوب + وضع داكن/فاتح.
- `script.js` – وظائف بسيطة (قائمة، تنشيط روابط، reveal، تبديل الثيم).
- `assets/` – ضع صورك (webp/jpg).

## الاستضافة المجانية / برسوم رمزية
### 1) GitHub Pages (مجاني)
1. أنشئ مخزن (Repository) باسم مثل `onepage-site`.
2. ارفع كل ملفات المشروع إلى الفرع `main` في الجذر.
3. من الإعدادات > Pages اختر المصدر `Deploy from a branch` وحدد `main` ومسار `/root`.
4. سيظهر رابط موقعك خلال دقائق.

### 2) Netlify (مجاني مع مميزات مفيدة)
- ادخل Netlify وسجّل دخولك بـ GitHub.
- اختر "New site from Git" وحدد الريبو.
- الإعدادات الافتراضية تكفي (Site is static).
- ميزة: نموذج التواصل يعمل تلقائياً بفضل `data-netlify="true"`.

### 3) Vercel / Cloudflare Pages (مجاني/رمزي)
- أنشئ مشروع جديد واربطه بالريبو. لا حاجة لأوامر بناء.
- مناسب جداً للمواقع الثابتة.

## تخصيص سريع
- غيّر العنوان والوصف داخل `<head>`.
- استبدل الشعار والنصوص بـ اسم نشاطك.
- ضع صورك في `assets/` وعدّل المسارات.
- لتفعيل إرسال النموذج لبريدك على Netlify: من لوحة التحكم > Forms.

## تحسينات إضافية (اختياري)
- ضغط الصور إلى `webp`، واستخدم أبعاد مناسبة.
- أضف نطاق (Domain) مخصص من مزودك أو عبر Netlify/Vercel.
- إن أردت بريد مباشر بدون Netlify، جرّب Formspree وغير `form` إلى `action="https://formspree.io/f/XXXX" method="POST"`.

بالتوفيق! 👍
